[package]
name = "inject_guide"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
regex = "1.10"
chrono = "0.4"
dirs = "6.0"
walkdir = "2.5"

[profile.release]
opt-level = 3
lto = true
strip = true
